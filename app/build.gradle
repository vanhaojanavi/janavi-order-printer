plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "vn.janavi.orderprinter"
    compileSdk = 34

    defaultConfig {
        applicationId = "vn.janavi.orderprinter"
        minSdk = 23
        targetSdk = 34
        versionCode = 2
        versionName = "1.1"
    }

    buildFeatures {
        viewBinding = true
    }

    signingConfigs {
        release {
            storeFile = file(System.getenv("JANAVI_KEYSTORE_PATH") ?: "keystore/janavi-release.keystore")
            storePassword = System.getenv("JANAVI_KEYSTORE_PASSWORD") ?: ""
            keyAlias = "janavi"
            keyPassword = System.getenv("JANAVI_KEYSTORE_PASSWORD") ?: ""
        }
    }

    buildTypes {
        debug {
            signingConfig = signingConfigs.getByName("release")
        }
        release {
            minifyEnabled = false
            signingConfig = signingConfigs.getByName("release")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.activity:activity-ktx:1.9.2")
    implementation("com.sunmi:printerlibrary:1.0.16")
}
